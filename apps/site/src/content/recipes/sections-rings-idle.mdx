---
title: "Dynamic sectors, rings and idle"
group: shapes
oneLiner: Sector shares that move step by step, an inner ring spun by the outer one, and a wheel that idles under the reels.
order: 40
tags:
  - dynamic
  - rings
  - subwheel
  - idle
---

## Jackpot sectors that move

The studio brief: solid-fill sectors whose arcs change step by step as a cosmetic state, with the titles sliding to the middle of whatever arc they own. The landing is decided by the server and never by the arcs.

<RecipeDemo code="dynamic-jackpot-steps" height={380} />

## Steps on a grid

Think of the rim as slots. Integer weights are the slots, so a step moves a border by exactly one slot and the static sectors never drift.

<RecipeDemo code="dynamic-quantized" />

## Weights from server state

No step list: the server sends a state with every round and the client animates `setWeights` to it.

<RecipeDemo code="dynamic-from-state" />

## Two rings

An outer ring of tiers and an inner ring read by its own inward-facing pointer at three o'clock. The rings spin independently and share one event stream.

<RecipeDemo code="two-ring-jackpot" height={420} />

## The outer ring triggers the inner one

Landing on SPIN starts the inner ring from the landing event; anything else ends the round.

<RecipeDemo code="outer-triggers-inner" height={420} />

## A wheel under the reels

A bonus wheel parked behind the reel panel, a third of it peeking out on the right and idling. The feature slides it out to show half, spins it from its idle speed, lands on the tongue at three o'clock, and slides it back under. Idle resumes on its own.

<RecipeDemo code="idle-side-wheel" height={380} />

## Idle on demand

One press per step: start the idle, spin from it (the idle resumes on its own after the landing), stop it with a ramp.

<RecipeDemo code="idle-controls" height={420} />
