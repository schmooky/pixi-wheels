---
title: "Starters"
group: starters
oneLiner: A wheel in ten lines, the two-colour gamble, and a template round-tripped through JSON.
order: 10
tags:
  - starter
  - gamble
  - template
---

The shapes most wheels start from. Every demo here is complete: copy it, swap the sections, ship.

## A wheel in ten lines

Unequal arcs, a server that decides by odds rather than by arc, and `setResult({ value })` landing on any section carrying that value.

<RecipeDemo code="basic-wheel" />

## Gamble, red and green

Two sections, random landing so the pointer never rests on the divider, and the quick profile because a gamble is spun again and again.

<RecipeDemo code="gamble-red-green" />

## From a template, and back to JSON

`WheelTemplates` are plain configs with their server adapter attached. `WheelBuilder.fromConfig()` builds one, `toConfig()` gives it back; the console shows the JSON, which is the shape the studio exports.

<RecipeDemo code="from-template" />
