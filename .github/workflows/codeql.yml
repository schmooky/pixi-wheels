name: CodeQL

# GitHub-native static analysis for JavaScript/TypeScript across the
# whole monorepo. Findings land under the "Security" tab of the repo and
# are attached as annotations on PRs that introduce a regression.

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
      # Long-lived integration branch. Without it every PR that targets v2
      # skipped CodeQL entirely.
      - v2
  schedule:
    # Mondays 07:00 UTC - same slot as dependabot, so "weekly review" is
    # one pass across deps + SAST.
    - cron: '0 7 * * 1'

permissions:
  contents: read

concurrency:
  group: codeql-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      actions: read
      contents: read

    strategy:
      fail-fast: false
      matrix:
        language:
          - javascript-typescript

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Initialize CodeQL
        uses: github/codeql-action/init@cdf488f595d80d6e07e03d4674febd5ab45fa938 # v4.37.9
        with:
          languages: ${{ matrix.language }}
          queries: security-and-quality

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@cdf488f595d80d6e07e03d4674febd5ab45fa938 # v4.37.9
        with:
          category: '/language:${{ matrix.language }}'
